import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Debt Settlement Program | Settle In Peace', description: 'Track your debt settlement program progress, settlements, milestones, and payments.', alternates: { canonical: '/settlement' } };
export default function SettlementLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }

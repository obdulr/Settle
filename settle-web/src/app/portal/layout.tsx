import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Provider Portal | Settle In Peace', description: 'Manage qualified debt relief leads, credits, and provider settings.', alternates: { canonical: '/portal' } };
export default function PortalLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }

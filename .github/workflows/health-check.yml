name: Health Check & Keep-Alive

on:
  schedule:
    # Every 10 minutes — keeps Railway free-tier services awake
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  health-check:
    name: Ping production endpoints
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          echo "Pinging SettleInPeace API..."
          STATUS=$(curl -sf -o /dev/null -w "%{http_code}" https://api.settleinpeace.com/health || echo "000")
          echo "API health: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "::warning::API health check failed ($STATUS)"
          fi

      - name: Ping Web
        run: |
          echo "Pinging SettleInPeace web..."
          STATUS=$(curl -sf -L -o /dev/null -w "%{http_code}" https://settleinpeace.com || echo "000")
          echo "Web health: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "::warning::Web health check failed ($STATUS)"
          fi

      - name: Ping Web (www)
        run: |
          echo "Pinging www.settleinpeace.com..."
          STATUS=$(curl -sf -L -o /dev/null -w "%{http_code}" https://www.settleinpeace.com || echo "000")
          echo "Web (www): $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "::warning::Web www health check failed ($STATUS)"
          fi
